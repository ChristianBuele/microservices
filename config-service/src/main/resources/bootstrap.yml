server:
    port: 8081
spring:
    cloud:
        config:
            server:
                git:
                    uri: https://github.com/ChristianBuele/microservices.git
                    searchPath: config-data
                    username: ${GIT_USER}
                    password: ${GIT_PASSWORD}
    security:
        user:
            name: root
            password: s3cr3t
server:
  port: 8099

eureka:
  instance:
    hostname: https://registry-service-spring.herokuapp.com
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/